# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# local notes / keys (never commit secrets)
*.local
secrets.*

# Web Store screenshot staging — curate before adding any to the repo
/screenshots/
